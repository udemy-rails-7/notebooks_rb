<div>
    <h1><%= @note.title %></h1>
    <p><%= @note.content %></p>
    <%= link_to 'Edit', edit_note_path(@note) %> |
    <%= link_to 'Back', notes_path %>
    <%=button_to 'Delete', note_path(@note), method: :delete, data: { confirm: 'Are you sure?' } %>
    </div>